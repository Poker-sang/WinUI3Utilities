<Window
    x:Class="WinUI3Utilities.Test.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="using:WinUI3Utilities"
    xmlns:local="using:WinUI3Utilities.Test"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="using:WinUI3Utilities.Test.Pages"
    mc:Ignorable="d">

    <Grid>
        <Border
            x:Name="TitleBar"
            Height="{x:Bind NavigationView.CompactPaneLength}"
            VerticalAlignment="Top"
            IsHitTestVisible="True">
            <StackPanel
                Margin="12"
                Orientation="Horizontal"
                Spacing="14">
                <FontIcon
                    Width="25"
                    Height="25"
                    Glyph="&#xEA3A;" />
                <TextBlock
                    x:Name="TitleTextBlock"
                    VerticalAlignment="Center"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{x:Bind helper:CurrentContext.Title}" />
            </StackPanel>
        </Border>
        <NavigationView
            x:Name="NavigationView"
            BackRequested="BackRequested"
            DisplayModeChanged="{x:Bind helper:TitleBarHelper.DisplayModeChanged}"
            IsBackButtonVisible="Visible"
            IsTabStop="False"
            ItemInvoked="ItemInvoked"
            Loaded="Loaded"
            PaneClosing="{x:Bind helper:TitleBarHelper.PaneClosing}"
            PaneOpening="{x:Bind helper:TitleBarHelper.PaneOpening}">
            <NavigationView.MenuItems>
                <NavigationViewItem
                    Content="主页"
                    Icon="Home"
                    Tag="{x:Bind pages:IndexPage.TypeGetter}" />
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem
                    Content="用户"
                    Icon="People"
                    Tag="{x:Bind pages:IndexPage.TypeGetter}" />
            </NavigationView.FooterMenuItems>
            <Frame x:Name="NavigateFrame" />
        </NavigationView>
        <TeachingTip Loaded="TeachingTipOnLoaded" />
    </Grid>
</Window>
